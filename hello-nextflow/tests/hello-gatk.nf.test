nextflow_pipeline {

    name "Test Workflow hello-gatk.nf"
    script "hello-gatk.nf"

    test("Should run without failures") {

        when {
            params {
                outdir = "tests/results"
            }
        }

        then {
            assert workflow.success
        }

    }

}
